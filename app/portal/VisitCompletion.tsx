"use client";
import {FormEvent,useState} from "react";
export default function VisitCompletion({eventId,household,dishes,onSaved}:{eventId:number;household:string;dishes:string[];onSaved:()=>Promise<void>}){const [checks,setChecks]=useState({countersClean:false,sinkClean:false,trashHandled:false,appliancesOff:false}),[busy,setBusy]=useState(false),[message,setMessage]=useState("");const ready=Object.values(checks).every(Boolean);async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setBusy(true);setMessage("");const form=new FormData(event.currentTarget);form.set("scheduleEventId",String(eventId));Object.entries(checks).forEach(([key,value])=>form.set(key,String(value)));try{const response=await fetch("/api/chef/visit-completion",{method:"POST",body:form}),data=await response.json() as {error?:string};if(!response.ok)throw new Error(data.error||"Upload failed");setMessage("Visit completed. Photos and cleanup confirmation are now in the customer’s account.");await onSaved()}catch(error){setMessage(error instanceof Error?error.message:"Upload failed. Please try again.")}finally{setBusy(false)}}return <form className="visit-completion" onSubmit={submit}><header><small>REQUIRED BEFORE COMPLETION</small><h3>Photograph meals and the clean kitchen</h3><p>These photos give {household} a clear visit update and show the kitchen was left clean.</p></header><div className="photo-inputs"><label>Dish photos<input name="dishPhotos" type="file" accept="image/jpeg,image/png,image/webp,image/heic,image/heif" capture="environment" multiple required/><small>Take 1–8 clear photos of the finished dishes.</small></label><label>Clean-kitchen photo<input name="kitchenPhotos" type="file" accept="image/jpeg,image/png,image/webp,image/heic,image/heif" capture="environment" required/><small>Show counters, sink, and the finished work area.</small></label></div>{dishes.length?<label>Dish shown first<select name="dishTitle" defaultValue={dishes[0]}>{dishes.map(dish=><option key={dish}>{dish}</option>)}</select></label>:null}<fieldset><legend>Cleanup confirmation</legend>{([['countersClean','Counters and work surfaces cleaned'],['sinkClean','Sink and used cooking area cleaned'],['trashHandled','Food waste and trash handled'],['appliancesOff','Appliances checked and left safe']] as const).map(([key,label])=><label key={key}><input type="checkbox" checked={checks[key]} onChange={event=>setChecks(current=>({...current,[key]:event.target.checked}))}/><span>{label}</span></label>)}</fieldset><label>Visit note<textarea name="notes" maxLength={1000} placeholder="Optional note for the customer about meals, storage, or the kitchen."/></label><button disabled={!ready||busy}>{busy?"Uploading securely…":"Finish visit and share update →"}</button>{message?<p className={message.startsWith("Visit completed")?"success":"error"} role="status">{message}</p>:null}</form>}
