"use client";
import {FormEvent,useState} from "react";
export default function InquiryForm(){const[state,setState]=useState<"idle"|"saving"|"done"|"error">("idle");async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setState("saving");const form=event.currentTarget,data=Object.fromEntries(new FormData(form));const response=await fetch("/api/private-chef",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(data)});setState(response.ok?"done":"error");if(response.ok)form.reset()}if(state==="done")return <div className="pc-form-success"><b>✓</b><h3>Your dinner request is in.</h3><p>We’ll review the date and details, then contact you to begin planning your menu.</p><button onClick={()=>setState("idle")}>Send another request</button></div>;return <form onSubmit={submit}><label>Your name<input required name="fullName" placeholder="First and last name"/></label><div><label>Email<input required type="email" name="email" placeholder="you@example.com"/></label><label>Phone<input type="tel" name="phone" placeholder="(503) 555-0123"/></label></div><div><label>Preferred date<input required type="date" name="preferredDate"/></label><label>Guest count<input required type="number" min="2" max="40" name="guestCount" placeholder="6"/></label></div><label>Where will dinner be held?<input required name="location" placeholder="City or vacation property"/></label><label>Occasion<input name="occasion" placeholder="Anniversary, birthday, vacation dinner…"/></label><label>Tell us about the evening<textarea name="details" rows={4} placeholder="The feeling, favorite foods, dietary needs, and anything we should know…"/></label><button disabled={state==="saving"}>{state==="saving"?"Sending…":"Request a private-chef proposal"} <span>→</span></button>{state==="error"?<p className="pc-form-error">We couldn’t send that request. Please check the required fields and try again.</p>:null}<small>We’ll confirm availability before creating your menu.</small></form>}
